# LangChain.js + Gemini sidecar.
FROM node:22-alpine
WORKDIR /app

# Install production deps first (cached unless package*.json changes).
COPY package*.json ./
RUN npm ci --omit=dev

COPY server.js ./

ENV NODE_ENV=production
ENV PORT=8090
EXPOSE 8090

CMD ["node", "server.js"]
